name: clang-tidy-differential

on:
  workflow_call:
    inputs:
      container:
        required: false
        default: ghcr.io/autowarefoundation/autoware:universe-dependencies-jazzy
        type: string
      rosdistro:
        type: string
        default: jazzy
        required: false
      # Why is it a JSON string?
      # https://github.com/orgs/community/discussions/11692#discussioncomment-3541856
      runner:
        default: "['ubuntu-24.04']"
        required: false
        type: string
      run-condition:
        default: true
        required: false
        type: boolean

env:
  BUILD_TYPE_CUDA_STATE: ${{ contains(inputs.container, '-cuda') && 'cuda' || 'nocuda' }}

jobs:
  clang-tidy-differential:
    if: ${{ inputs.run-condition }}
    runs-on: ${{ fromJson(inputs.runner) }}
    container: ${{ inputs.container }}
    steps:
      - name: Set PR fetch depth
        run: echo "PR_FETCH_DEPTH=$(( ${{ github.event.pull_request.commits }} + 1 ))" >> "${GITHUB_ENV}"

      - name: Checkout PR branch and all PR commits
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: ${{ env.PR_FETCH_DEPTH }}

      - name: Show machine specs
        run: lscpu && free -h
        shell: bash

      - name: Show disk space before the tasks
        run: df -h
        shell: bash

      - name: Remove exec_depend
        uses: autowarefoundation/autoware-github-actions/remove-exec-depend@v1

      - name: Check if clang-tidy config changed
        id: check-config-changed
        run: |
          if git diff --name-only "origin/${{ github.base_ref }}"...HEAD | grep -q '^\.clang-tidy-ci$'; then
            echo "config-changed=true" >> $GITHUB_OUTPUT
          else
            echo "config-changed=false" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Get self packages
        id: get-self-packages
        if: ${{ steps.check-config-changed.outputs.config-changed == 'true' }}
        uses: autowarefoundation/autoware-github-actions/get-self-packages@v1

      - name: Get modified packages
        id: get-modified-packages
        if: ${{ steps.check-config-changed.outputs.config-changed == 'false' }}
        uses: autowarefoundation/autoware-github-actions/get-modified-packages@v1

      - name: Get changed files (existing files only)
        id: get-changed-files
        if: ${{ steps.check-config-changed.outputs.config-changed == 'false' }}
        run: |
          echo "changed-files=$(git diff --name-only "origin/${{ github.base_ref }}"...HEAD | grep -E '\.(cpp|hpp)$' | while read -r file; do [ -e "$file" ] && echo -n "$file "; done)" >> $GITHUB_OUTPUT
        shell: bash

      - name: Prepare build_depends.repos file (main branch)
        if: ${{ github.event.pull_request.base.ref != 'humble' && github.event.pull_request.base.ref != 'jazzy' }}
        uses: ./.github/actions/combine-repos-action
        with:
          base_file: build_depends_stable.repos
          overlay_file: build_depends_nightly.repos
          output_file: build_depends.repos

      - name: Prepare build_depends.repos file (humble/jazzy branch)
        if: ${{ github.event.pull_request.base.ref == 'humble' || github.event.pull_request.base.ref == 'jazzy' }}
        run: cp build_depends_stable.repos build_depends.repos
        shell: bash

      - name: Remove released Debian packages of the source-built dependencies
        if: ${{ steps.check-config-changed.outputs.config-changed == 'true' || steps.get-changed-files.outputs.changed-files != '' }}
        run: |
          ros_distro="${ROS_DISTRO:-${{ inputs.rosdistro }}}"
          # Same step as in build-and-test-differential.yaml: the released packages install
          # headers under /opt/ros/$ROS_DISTRO/include, which shadow the source-built ones.
          # The clang-tidy action reuses the build cache from that job, so the workspace must
          # match it. The action's own vcs import finds dependency_ws already populated.
          mkdir -p dependency_ws
          vcs import --recursive dependency_ws < build_depends.repos
          for pkg_xml in $(find . -name package.xml); do
            pkg_name=$(xmllint --xpath 'string(//package/name)' "$pkg_xml")
            deb_name="ros-${ros_distro}-${pkg_name//_/-}"
            if dpkg-query -W -f='${Status}' "$deb_name" 2>/dev/null | grep -q '^install ok installed$'; then
              echo "Removing $deb_name"
              sudo dpkg --remove --force-depends "$deb_name"
            fi
          done
        shell: bash

      - name: Run clang-tidy
        if: ${{ steps.check-config-changed.outputs.config-changed == 'true' || steps.get-changed-files.outputs.changed-files != '' }}
        uses: autowarefoundation/autoware-github-actions/clang-tidy@v1
        with:
          rosdistro: ${{ inputs.rosdistro }}
          target-packages: ${{ steps.check-config-changed.outputs.config-changed == 'true' && steps.get-self-packages.outputs.self-packages || steps.get-modified-packages.outputs.modified-packages }}
          clang-tidy-config-url: https://raw.githubusercontent.com/${{ github.event.pull_request.head.repo.full_name }}/${{ github.event.pull_request.head.sha }}/.clang-tidy-ci
          clang-tidy-ignore-path: .clang-tidy-ignore
          build-depends-repos: build_depends.repos
          cache-key-element: ${{ env.BUILD_TYPE_CUDA_STATE }}
          artifact-name: clang-tidy-result-${{ inputs.rosdistro }}${{ contains(inputs.container, '-cuda') && '-cuda' || '' }}
          use-clang-tidy-cache: true
          # Core dependencies are provided pre-built by the Docker image under /opt/autoware.
          underlay-workspace: /opt/autoware

      - name: Show disk space after the tasks
        run: df -h
